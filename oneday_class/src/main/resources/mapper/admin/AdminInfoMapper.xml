<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.oneclass.admin.admininfo.AdminInfoDAO">

	<select id="selectAdminInfo"
		resultType="kr.co.oneclass.admin.admininfo.AdminInfoDTO">

		SELECT
		MANAGER_CODE,
		ID,
		PASSWORD,
		NAME,
		EMAIL

		FROM MANAGER

		WHERE MANAGER_CODE =
		#{managerCode}

	</select>


	<select id="selectAdminPassword" resultType="string">

		SELECT PASSWORD

		FROM MANAGER

		WHERE MANAGER_CODE =
		#{managerCode}

	</select>


	<update id="updateAdminInfo">

		UPDATE MANAGER

		SET NAME = #{name},
		EMAIL = #{email}

		WHERE MANAGER_CODE =
		#{managerCode}

	</update>


	<update id="updateAdminInfoWithPassword">

		UPDATE MANAGER

		SET NAME = #{name},
		EMAIL = #{email},
		PASSWORD = #{encodedPassword}

		WHERE MANAGER_CODE =
		#{managerCode}

	</update>

</mapper>
