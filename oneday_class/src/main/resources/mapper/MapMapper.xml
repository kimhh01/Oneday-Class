<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.oneclass.map.MapDAO">

   <select id="selectClassList" parameterType="kr.co.oneclass.map.MapSearchDTO" resultType="kr.co.oneclass.common.ClassDTO">
    SELECT 
        class_code AS classCode,
        operator_code AS operatorCode,
        category_code AS categoryCode,
        name,
        price,
        desired_price AS desiredPrice,
        zipcode,
        address,
        old_address AS oldAddress,
        address2,
        ingredients_include AS ingredientsInclude,
        status,
        review_sum AS reviewSum,
        star_rating_avg AS starRatingAvg,
        views_cnt AS viewsCnt,
        single_introduce AS singleIntroduce,
        introduce,
        finish_introduce AS finishIntroduce,
        lat,
        lng,
        approval_status AS approvalStatus,
        approval_memo AS approvalMemo,
        write_date AS writeDate,
        register_step AS registerStep  <!-- 💡 추가! -->
    FROM class
    <where>
        <!-- 위경도 값이 들어왔을 때만 영역 필터링 -->
        <if test="minLat != null and minLat != 0 and maxLat != null and maxLat != 0">
            AND lat BETWEEN #{minLat} AND #{maxLat}
        </if>
        <if test="minLng != null and minLng != 0 and maxLng != 0 and maxLng != 0">
            AND lng BETWEEN #{minLng} AND #{maxLng}
        </if>
    </where>
    ORDER BY class_code DESC
</select>

    <!-- 2. 클래스 상세 정보 조회 -->
    <select id="selectClassDetail" parameterType="int" resultType="kr.co.oneclass.common.ClassDTO">
        SELECT * FROM class WHERE class_code = #{classCode}
    </select>

    <!-- 3. 위치 기반 지도 검색 목록 조회 -->
    <select id="search" resultType="kr.co.oneclass.map.MapSearchDTO">
        SELECT * FROM class
        WHERE lat BETWEEN #{minLat} AND #{maxLat}
          AND lng BETWEEN #{minLng} AND #{maxLng}
    </select>

</mapper>