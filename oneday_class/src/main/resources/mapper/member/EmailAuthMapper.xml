<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.oneclass.member.EmailAuthDAO">

    <!-- 결과 맵핑 정의 -->
    <resultMap id="emailAuthResultMap" type="kr.co.oneclass.member.EmailAuthDomain">
        <result property="email" column="email"/>
        <result property="authCode" column="authCode"/>
        <result property="type" column="type"/>
        <result property="issueDate" column="issueDate"/>
    </resultMap>

    <!-- 이메일 인증 정보 등록 -->
    <insert id="insertEmailAuth" parameterType="kr.co.oneclass.member.EmailAuthDTO">
        INSERT INTO email_auth (email, auth_code, type, issue_date) 
        VALUES (#{email}, #{authCode}, #{type}, SYSDATE)
    </insert>

    <!-- 이메일 인증 정보 조회 (가장 최근 1건) -->
    <select id="selectEmailAuth" resultMap="emailAuthResultMap">
        SELECT * FROM (
            SELECT email, auth_code AS authCode, type, issue_date AS issueDate 
            FROM email_auth 
            WHERE email = #{email} 
            <if test="type != null and type != ''">
                AND type = #{type} 
            </if>
            ORDER BY issue_date DESC
        ) WHERE ROWNUM = 1
    </select>

</mapper>